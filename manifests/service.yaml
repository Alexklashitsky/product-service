apiVersion: v1
kind: Service
metadata:
  name: testdeployment
  namespace: namespace-workflow-1718348071671
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: testdeployment
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002
